-- Pattern NN: <name>            (10-sql.md section 7)
-- Trigger: <the phrasing in a question that should make you reach for this>
-- Dialect: SQLite 3.50

-- Problem statement in one sentence, against the local schema:
--   <e.g. "for each lot, return the most recent bid">


-- Version A -- window functions




-- Version B -- without window functions




-- Do they return identical rows? Prove it, don't assume:
--   <version A> EXCEPT <version B>   -- plus the reverse; both must be empty.


-- EXPLAIN QUERY PLAN notes
--   before index:
--   after index:
--   index that mattered:


-- Trap that bit me:
